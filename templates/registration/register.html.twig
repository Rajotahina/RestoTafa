{% extends 'base.html.twig' %}

{% block title %}Register
{% endblock %}

{% block body %}
	<div
		class="container">

		{# {{ form_start(registrationForm) }}
		{{ form_row(registrationForm.username) }}
		    {{ form_row(registrationForm.email) }}
		    {{ form_row(registrationForm.plainPassword, {
		        label: 'Password'
		    }) }}
		    {{ form_row(registrationForm.agreeTerms) }}
		
		    <button type="submit" class="btn">Register</button>
		{{ form_end(registrationForm) }} #}
		<div class="container">
			<div class="col-md-6 mx-auto text-center">
				<div class="header-title">
					<h1 class="wv-heading--title">
						Check out — it’s free!
					</h1>
					<h2 class="wv-heading--subtitle">
						créer un compte pour voir nos produits...
					</h2>
				</div>
			</div>
			<div class="row">
				<div class="col-md-4 mx-auto">
					<div class="myform form ">
						{{ form_start(registrationForm) }}
						<div class="form-group">
							<div class="form-control my-input" id="name" placeholder="Name">
								{{ form_row(registrationForm.username) }}
							</div>
						</div>
						<div class="form-group">
							<div class="form-control my-input" id="email" placeholder="Email">
								{{ form_row(registrationForm.email) }}
							</div>
						</div>
						<div class="form-group">
							<div min="0" name="phone" id="phone" class="form-control my-input" placeholder="Phone">
								{{ form_row(registrationForm.plainPassword, {
                        label: 'Password'
                    }) }}
							</div>
						</div>
						<div class="form-control my-input" id="email" placeholder="Email">
							{{ form_row(registrationForm.agreeTerms) }}
						</div>
						<p class="small mt-3">By signing up, you are indicating that you have read and agree to the
							<a href="#" class="ps-hero__content__link">Terms of Use</a>
							and
							<a href="#">Privacy Policy</a>.
						</p>
						<div class="text-center ">
							<button type="submit" class=" btn btn-primary send-button tx-tfm" value="">
								Create Your Free Account
							</button>
							{{ form_end(registrationForm) }}
						</div>
						<div class="col-md-12 ">
							<div class="login-or">
								<hr class="hr-or">
								<span class="span-or">or</span>
							</div>
						</div>
						{# <div class="form-group">
							<a class="btn btn-block g-button" href="#">
								<i class="fa fa-google"></i>
								Sign up with Google
							</a>
						</div> #}

					</form>
				</div>
			</div>
		</div>
	</div>
</div>{% endblock %}

